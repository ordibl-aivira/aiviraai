FROM python:3.12-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY packages/ packages/
COPY apps/agent_runtime/ apps/agent_runtime/
ENV PYTHONPATH=/app
CMD ["uvicorn", "apps.agent_runtime.app.main:app", "--host", "0.0.0.0", "--port", "8001"]
