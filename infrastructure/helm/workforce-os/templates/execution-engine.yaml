apiVersion: apps/v1
kind: Deployment
metadata:
  name: execution-engine
  labels:
    app: execution-engine
    layer: execution
    {{- include "workforce-os.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.services.executionEngine.replicas }}
  selector:
    matchLabels:
      app: execution-engine
  template:
    metadata:
      labels:
        app: execution-engine
        layer: execution
    spec:
      containers:
        - name: execution-engine
          image: {{ include "workforce-os.image" (dict "Values" .Values "svc" "execution-engine") }}
          imagePullPolicy: {{ .Values.global.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.services.executionEngine.port }}
          env:
            - name: PYTHONPATH
              value: /app
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.services.executionEngine.port }}
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.services.executionEngine.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: execution-engine
spec:
  selector:
    app: execution-engine
  ports:
    - port: {{ .Values.services.executionEngine.port }}
      targetPort: {{ .Values.services.executionEngine.port }}
