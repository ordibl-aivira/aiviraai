apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  labels:
    app: auth-service
    {{- include "workforce-os.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.services.authService.replicas }}
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      containers:
        - name: auth-service
          image: {{ include "workforce-os.image" (dict "Values" .Values "svc" "auth-service") }}
          imagePullPolicy: {{ .Values.global.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.services.authService.port }}
          env:
            - name: PYTHONPATH
              value: /app
            - name: POSTGRES_DSN
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: dsn
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.services.authService.port }}
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.services.authService.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: auth-service
spec:
  selector:
    app: auth-service
  ports:
    - port: {{ .Values.services.authService.port }}
      targetPort: {{ .Values.services.authService.port }}
