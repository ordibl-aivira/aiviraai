apiVersion: apps/v1
kind: Deployment
metadata:
  name: content-agent
  labels:
    app: content-agent
    layer: thinking
    {{- include "workforce-os.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.services.contentAgent.replicas }}
  selector:
    matchLabels:
      app: content-agent
  template:
    metadata:
      labels:
        app: content-agent
        layer: thinking
    spec:
      containers:
        - name: content-agent
          image: {{ include "workforce-os.image" (dict "Values" .Values "svc" "content-agent") }}
          imagePullPolicy: {{ .Values.global.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.services.contentAgent.port }}
          env:
            - name: PYTHONPATH
              value: /app
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.services.contentAgent.port }}
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.services.contentAgent.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: content-agent
spec:
  selector:
    app: content-agent
  ports:
    - port: {{ .Values.services.contentAgent.port }}
      targetPort: {{ .Values.services.contentAgent.port }}
