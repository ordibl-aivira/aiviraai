apiVersion: apps/v1
kind: Deployment
metadata:
  name: memory-service
  labels:
    app: memory-service
    {{- include "workforce-os.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.services.memoryService.replicas }}
  selector:
    matchLabels:
      app: memory-service
  template:
    metadata:
      labels:
        app: memory-service
    spec:
      containers:
        - name: memory-service
          image: {{ include "workforce-os.image" (dict "Values" .Values "svc" "memory-service") }}
          imagePullPolicy: {{ .Values.global.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.services.memoryService.port }}
          env:
            - name: PYTHONPATH
              value: /app
            - name: POSTGRES_DSN
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: dsn
            - name: REDIS_URL
              value: redis://redis:6379/0
            - name: QDRANT_URL
              value: http://qdrant:6333
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.services.memoryService.port }}
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.services.memoryService.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: memory-service
spec:
  selector:
    app: memory-service
  ports:
    - port: {{ .Values.services.memoryService.port }}
      targetPort: {{ .Values.services.memoryService.port }}
